import 'package:flutter/material.dart';

class CancelledTask extends StatelessWidget {
  const CancelledTask({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Cancelled  Task')),
    );
  }
}
